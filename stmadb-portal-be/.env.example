# Backend environment variables example
# Update these values on the production server

# Public base URL where uploads are served (no trailing slash)
BASE_URL=https://apps.smkn1adw.sch.id

# Optional: path to uploads directory (relative to project root)
# By default server uses: ./uploads and serves it at /uploads
# UPLOADS_PATH=./uploads

# Database connection (example - keep your credentials secret)
DATABASE_URL=postgresql://user:password@localhost:5432/stmadb_portal?schema=public

# JWT, other secrets
JWT_SECRET=replace-with-strong-secret

# Prisma client
# PRISMA_CLIENT_ENGINE_TYPE=node
